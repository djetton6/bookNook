import { ok } from "assert";

/*
it should:
    return the title of the book
    return the authors of the book
    return the publishing house of the book
    return the image of the book
*/

test("Title is loaded correctly", function() {
    ok(response.items)
})